<% content_for :title do %>
All the Tags
<% end %>

<h1>Tags</h1>

<ul class="object-list tags-list">
<% @tags.each do |tag| %>
  <li>
    <%= link_to stories_tag_path(tag.tag) do %>
      <%= tag.tag %> (<%= tag.story_count %>)
    <% end %>
  </li>
<% end %>
</ul>

<%= will_paginate @tags %>
